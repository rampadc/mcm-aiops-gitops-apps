apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name: managedcluster-aws-tokyo
spec:
  componentKinds:
    - group: apps.open-cluster-management.io
      kind: Subscription
  descriptor: {}
  selector:
    matchExpressions:
      - key: app
        operator: In
        values:
          - managedcluster-aws-tokyo