apiVersion: apps.open-cluster-management.io/v1
kind: Subscription
metadata:
  annotations:
    apps.open-cluster-management.io/github-path: clusters/cluster-build/aws/aws-tokyo 
    apps.open-cluster-management.io/reconcile-option: replace
  name: managedcluster-aws-tokyo
spec:
  channel: openshift-gitops/managedcluster-aws-tokyo
  placement:
    placementRef:
      name: managedcluster-aws-tokyo
      kind: PlacementRule